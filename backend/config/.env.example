# AI v1 uses Firebase Functions + Genkit stub flows and does not require an LLM API key.
# Add Vertex AI configuration later when the real Gemini provider is enabled.
# Match this to the exact Authorization header value configured in RevenueCat.
REVENUECAT_WEBHOOK_SECRET=
REVENUECAT_SECRET_API_KEY=
